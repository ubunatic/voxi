/voxi
/voxi-modifierd

# Test & coverage artifacts
*.test
*.out
*.prof

# Local audio recordings/caches
*.wav
*.mp3

# Editor / IDE artifacts
.DS_Store
.idea/
.vscode/
# Private local speech-context benchmark recordings.
testdata/speech-context/*.wav
*.lock

# Committed source asset, not a scratch recording (issue 101): embedded via
# Go //go:embed and played back as the modifier-release-buffering notice.
!internal/eager/notify/typing-paused.wav
