{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Voxi Monitor TUI Performance Tuning",
  "description": "Screen redraw rate and live mic loudness meter sampling/ballistics for `voxi monitor -w`.",
  "type": "object",
  "required": ["paint", "mic_level", "status_icon"],
  "properties": {
    "paint": {
      "type": "object",
      "description": "Screen redraw cadence, independent of --interval's slower CPU/GPU/process collection.",
      "required": ["fps"],
      "properties": {
        "fps": {
          "type": "number",
          "exclusiveMinimum": 0,
          "maximum": 60,
          "description": "Redraw rate in frames per second."
        }
      },
      "additionalProperties": false
    },
    "mic_level": {
      "type": "object",
      "description": "Live mic-level meter capture rate/chunking and VU-meter ballistics.",
      "required": ["sample_rate_hz", "chunk_ms", "window_ms", "attack_ms", "decay_ms"],
      "properties": {
        "sample_rate_hz": {
          "type": "integer",
          "minimum": 1000,
          "maximum": 48000,
          "description": "Raw PCM capture sample rate in Hz."
        },
        "chunk_ms": {
          "type": "number",
          "exclusiveMinimum": 0,
          "maximum": 1000,
          "description": "Duration of each raw-amplitude sample chunk, in milliseconds."
        },
        "window_ms": {
          "type": "number",
          "exclusiveMinimum": 0,
          "maximum": 5000,
          "description": "Rolling-window duration reduced to one target level per update, in milliseconds."
        },
        "attack_ms": {
          "type": "number",
          "exclusiveMinimum": 0,
          "maximum": 5000,
          "description": "Exponential time constant easing the displayed level up toward a louder target, in milliseconds. Always interpolates -- never jumps straight to the new value."
        },
        "decay_ms": {
          "type": "number",
          "exclusiveMinimum": 0,
          "maximum": 5000,
          "description": "Exponential time constant easing the displayed level down toward a quieter target, in milliseconds."
        }
      },
      "additionalProperties": false
    },
    "status_icon": {
      "type": "object",
      "description": "The monitor status line's leading icon (see internal/monitor/render.go's formatRecordState).",
      "required": ["always_show_loudness"],
      "properties": {
        "always_show_loudness": {
          "type": "boolean",
          "description": "false: icon is a static idle/transcribing glyph, only becoming the live loudness meter while actually recording. true: icon is always the live loudness glyph, even while idle."
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
