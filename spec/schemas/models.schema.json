{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Voxi ASR Model Registry",
  "description": "Whisper models Voxi may invoke via voxtype, and the per-model hallucination stop-word patterns used to filter transcripts.",
  "type": "object",
  "required": ["default_model", "speech_context", "transcript_safety", "models"],
  "properties": {
    "default_model": {
      "type": "string",
      "description": "Model name used when no --model flag is given. Must be a key in models."
    },
    "speech_context": {
      "type": "object",
      "description": "Bounded static terminology and limits for opt-in Whisper decoder context.",
      "required": ["prompt_prefix", "terms", "max_terms", "max_chars", "max_term_chars"],
      "properties": {
        "prompt_prefix": { "type": "string", "minLength": 1 },
        "terms": {
          "type": "array",
          "minItems": 1,
          "items": { "type": "string", "minLength": 1 }
        },
        "max_terms": { "type": "integer", "minimum": 1, "maximum": 100 },
        "max_chars": { "type": "integer", "minimum": 1, "maximum": 2000 },
        "max_term_chars": { "type": "integer", "minimum": 1, "maximum": 200 }
      },
      "additionalProperties": false
    },
    "transcript_safety": {
      "type": "object",
      "description": "Conservative pre-injection transcript circuit breaker limits, measured in Unicode characters.",
      "required": ["max_chars", "max_token_chars", "max_repeat_unit_chars", "min_repeat_count", "min_repeated_chars"],
      "properties": {
        "max_chars": { "type": "integer", "minimum": 1 },
        "max_token_chars": { "type": "integer", "minimum": 1 },
        "max_repeat_unit_chars": { "type": "integer", "minimum": 1 },
        "min_repeat_count": { "type": "integer", "minimum": 2 },
        "min_repeated_chars": { "type": "integer", "minimum": 1 }
      },
      "additionalProperties": false
    },
    "models": {
      "type": "object",
      "description": "Map of Whisper model name (as passed to voxtype --model) to its configuration.",
      "minProperties": 1,
      "additionalProperties": {
        "type": "object",
        "required": ["label", "engine", "stop_words"],
        "properties": {
          "label": {
            "type": "string",
            "description": "Human-readable name shown in the monitor TUI and CLI help."
          },
          "engine": {
            "type": "string",
            "description": "Inference engine backing this model. 'whisper' dispatches through voxtype; 'cohere-transcribe' dispatches through the crispasr binary (see issue 074).",
            "enum": ["whisper", "cohere-transcribe"]
          },
          "stop_words": {
            "type": "array",
            "description": "Case-insensitive regex fragments matching known hallucinated phrases this model tends to leak (e.g. YouTube outros, subtitle credits). Applied both to reject whole-utterance hallucinations and to strip trailing hallucinated tails from otherwise-genuine speech.",
            "items": {
              "type": "object",
              "required": ["id", "pattern"],
              "properties": {
                "id": { "type": "string", "minLength": 1 },
                "pattern": { "type": "string", "minLength": 1 }
              },
              "additionalProperties": false
            }
          },
          "requires_gpu": {
            "type": "boolean",
            "default": false,
            "description": "If true, this model must not run on the CPU backend: eager mode falls back to cpu_fallback (or refuses to start if unset) when no GPU render node is present, and `voxi bench` skips its cpu row instead of executing it. Set this when docs/BenchBaseline.md (or an equivalent measurement) shows CPU RTF > 1.0 (sub-realtime) for the model."
          },
          "cpu_fallback": {
            "type": "string",
            "description": "Model to substitute when requires_gpu is true and no GPU render node is present. Must name another entry in models, and that entry must not itself require a GPU. Ignored when requires_gpu is false."
          }
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
